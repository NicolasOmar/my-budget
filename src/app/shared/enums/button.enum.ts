export enum ButtonTypeEnum {
  submit = 'submit',
  reset = 'reset',
  button = 'button'
}

export enum ButtonColorEnum {
  primary = 'primary',
  violet = 'violet',
  positive = 'positive',
  negative = 'negative'
}
